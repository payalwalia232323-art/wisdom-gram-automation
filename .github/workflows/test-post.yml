name: Wisdom Gram Reel Test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  create-and-publish-reel:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          pip install pillow cloudinary moviepy

      - name: Create Reel video
        run: |
          python - <<'PY'
          from PIL import Image, ImageDraw, ImageFont
          import subprocess

          W, H = 1080, 1920

          img = Image.new("RGB", (W, H), (18, 18, 18))
          draw = ImageDraw.Draw(img)

          font = ImageFont.truetype(
              "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf",
              62
          )

          small = ImageFont.truetype(
              "/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf",
              32
          )

          quote = "The axe forgets, but the tree remembers."

          words = quote.split()
          lines = []
          current = ""

          for word in words:
              test = (current + " " + word).strip()
              if draw.textbbox((0, 0), test, font=font)[2] <= 850:
                  current = test
              else:
                  lines.append(current)
                  current = word

          if current:
              lines.append(current)

          y = 760

          for line in lines:
              box = draw.textbbox((0, 0), line, font=font)
              width = box[2] - box[0]

              draw.text(
                  ((W - width) / 2, y),
                  line,
                  font=font,
                  fill="white"
              )

              y += 95

          brand = "wisdom_gram"

          box = draw.textbbox((0, 0), brand, font=small)
          width = box[2] - box[0]

          draw.text(
              ((W - width) / 2, 1720),
              brand,
              font=small,
              fill="white"
          )

          img.save("frame.png")

          subprocess.run([
              "ffmpeg",
              "-y",
              "-loop", "1",
              "-i", "frame.png",
              "-t", "8",
              "-r", "30",
              "-vf", "format=yuv420p",
              "-c:v", "libx264",
              "-movflags", "+faststart",
              "wisdom-reel.mp4"
          ], check=True)
          PY

      - name: Upload Reel to Cloudinary
        id: cloudinary
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
        run: |
          python - <<'PY'
          import cloudinary
          import cloudinary.uploader
          import os

          cloudinary.config(
              cloud_name=os.environ["CLOUDINARY_CLOUD_NAME"],
              api_key=os.environ["CLOUDINARY_API_KEY"],
              api_secret=os.environ["CLOUDINARY_API_SECRET"]
          )

          result = cloudinary.uploader.upload(
              "wisdom-reel.mp4",
              public_id="wisdom-gram/reel-test",
              overwrite=True,
              resource_type="video"
          )

          print("Cloudinary Reel URL:", result["secure_url"])

          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write("video_url=" + result["secure_url"] + "\n")
          PY

      - name: Create Instagram Reel container
        id: container
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: |
          RESPONSE=$(curl -s -X POST \
            "https://graph.instagram.com/v15.0/${IG_USER_ID}/media" \
            -F "media_type=REELS" \
            -F "video_url=${{ steps.cloudinary.outputs.video_url }}" \
            -F "caption=The axe forgets, but the tree remembers. #wisdom #proverbs #mindset #lifequotes #wisdomgram" \
            -F "share_to_feed=true" \
            -F "access_token=${IG_ACCESS_TOKEN}")

          echo "$RESPONSE"

          CONTAINER_ID=$(echo "$RESPONSE" | python -c "import sys,json; print(json.load(sys.stdin)['id'])")

          echo "container_id=$CONTAINER_ID" >> "$GITHUB_OUTPUT"

      - name: Wait for Reel processing
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: |
          for i in {1..18}; do

            RESPONSE=$(curl -s \
              "https://graph.instagram.com/v15.0/${{ steps.container.outputs.container_id }}?fields=status_code,status&access_token=${IG_ACCESS_TOKEN}")

            echo "$RESPONSE"

            if echo "$RESPONSE" | grep -q '"status_code":"FINISHED"'; then
              echo "Reel is ready."
              exit 0
            fi

            if echo "$RESPONSE" | grep -q '"status_code":"ERROR"'; then
              echo "Instagram rejected the Reel."
              exit 1
            fi

            sleep 10
          done

          echo "Reel processing timed out."
          exit 1

      - name: Publish Reel
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: |
          RESPONSE=$(curl -s -X POST \
            "https://graph.instagram.com/v15.0/${IG_USER_ID}/media_publish" \
            -F "creation_id=${{ steps.container.outputs.container_id }}" \
            -F "access_token=${IG_ACCESS_TOKEN}")

          echo "$RESPONSE"
